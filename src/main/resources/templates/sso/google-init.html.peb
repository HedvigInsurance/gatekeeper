<!doctype html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport"
          content="width=device-width, user-scalable=yes, initial-scale=1.0, maximum-scale=1.0, minimum-scale=1.0" />
    <title>Single Sign On - Hedvig</title>
    <script src="https://apis.google.com/js/api:client.js"></script>
    <link rel="stylesheet" href="/assets/style-base.css" type="text/css" />
    <link rel="stylesheet" href="/assets/style-sso.css" type="text/css" />
</head>
<body class="sso-body">
<div class="main-wrapper">
    <div class="main">
        <h1>Employee sign in</h1>
        <img src="https://cdn.hedvig.com/identity/graphics/hedvig-symbol-color.svg" width="50" />
        <p>This is an internal system. If you found your way here, but don't know what this is please <a href="https://www.hedvig.com">carry on</a> 👜.</p>

        {% if error != "" %}
            <div class="grant-error">Invalid grant</div>
        {% endif %}

        <div class="h-google-signin">
            <button class="sign-in-button" id="sign-in">Sign in</button>
        </div>
    </div>
</div>

<script>
const button = document.getElementById('sign-in');

gapi.load('auth2', function () {
    const auth2 = gapi.auth2.init({
        client_id: '{{ clientId | escape('js') }}',
        cookiepolicy: 'single_host_origin',
    });

    auth2.attachClickHandler(
        button,
        {},
        function (googleUser) {
            window.location =
                '/sso/callback/google?id_token='
                + encodeURIComponent(googleUser.getAuthResponse().id_token)
                + '&redirect=' + encodeURIComponent('{{ redirect | escape('js') }}')

        }
    )
});
</script>
</body>
</html>
